-- React project to test music api
-- Tailwind was utilized in addition to normal css(for faster development time)
-- Zustand is also used for state management

-- Home, Playlist and Song pages are working fine!